A clean flat-design educational infographic, 16:9 landscape aspect ratio, titled at the top center in bold sans-serif: "Dropout vs DropPath". Below the title a small centered subtitle: "Same Bernoulli mask, different shape". The canvas is split into two equal side-by-side panels separated by a thin vertical divider line, so the eye reads left panel first, then right panel, then the two shape labels along the bottom. Left panel uses a muted blue accent, right panel uses a muted orange accent, on an off-white background. Dropped or disabled elements are drawn in light grey with dashed outlines; active elements are solid and saturated.

LEFT PANEL, header label "Dropout": show a 3D-looking tensor grid — a stack of three flat rectangular slices labeled at the side "batch of 3 samples", each slice drawn as a grid of small square cells about 6 by 8. Individual cells are scattered randomly across all three slices in grey with an X or blank fill, roughly one third of them, while the rest stay solid blue. The scattered-off cells must clearly be independent single cells, not whole rows or whole slices. A short caption under the grid: "Random cells turned off". A tiny side note text: "Every sample stays active".

RIGHT PANEL, header label "DropPath": show a vertical stack of three transformer-block diagrams side by side, one per batch sample, each labeled "Sample 1", "Sample 2", "Sample 3". Each diagram is a residual block: an input node at the bottom, a curved skip-connection arrow on the left going straight up to a circular plus sign at the top, and on the right a branch path going up through two rounded boxes labeled "Attention" and "MLP" into the same plus sign. For Sample 1 and Sample 3 the whole branch is solid orange with solid arrows. For Sample 2 the entire right branch — both boxes and all its arrows — is greyed out with dashed outlines and a large grey X over it, while only the skip connection remains solid, and a bold label beside it reads "x = x, identity". A caption under the three diagrams: "Whole residual branch off".

BOTTOM STRIP: under the left panel a monospace pill-shaped badge reading "mask (B, N, D)" with the small caption "per element"; under the right panel a monospace pill-shaped badge reading "mask (B, 1, 1)" with the small caption "per sample". Between them, centered at the very bottom, a small monospace formula line: "x = x + DropPath(f(x))".

Style: clean flat vector design, educational textbook infographic, generous whitespace, thin consistent stroke weights, rounded corners, limited palette of off-white, dark slate text, one blue, one orange, and light grey for disabled parts. All labels short, in crisp legible English sans-serif. No photographic texture, no gradients, no drop shadows.
