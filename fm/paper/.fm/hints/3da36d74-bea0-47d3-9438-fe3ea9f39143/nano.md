A clean flat-design educational infographic, 16:9 landscape, titled "BYOL vs DINO" in large bold text at the top center, with a small subtitle underneath reading "Same momentum teacher, different matching loss".

Layout: two large vertical comparison panels side by side, left panel labeled "BYOL" with a soft blue color theme, right panel labeled "DINO (디노)" with a soft orange color theme. A thin vertical divider line separates them. A narrow full-width strip runs across the bottom under both panels.

LEFT PANEL "BYOL": a top-to-bottom flow diagram of two parallel branches. The left branch is labeled "Online network" and stacks three rounded boxes top to bottom: "Encoder", "Projector", and a highlighted box "Predictor" drawn with a bold outline and a small star icon, with a tiny red caption beside it reading "Required". The right branch is labeled "Target network" and stacks only two rounded boxes: "Encoder", "Projector" — visibly one box shorter than the left branch, so the asymmetry is obvious. A horizontal arrow from the left branch to the right branch is labeled "EMA momentum". A small "stop-gradient" tag with a scissors icon sits on the target branch. The two branches converge at the bottom into a single pill-shaped badge reading "L2 + MSE loss" in blue.

RIGHT PANEL "DINO": the same top-to-bottom two-branch flow, but both branches are identical in height. Left branch labeled "Student", right branch labeled "Teacher", each stacking two rounded boxes: "Encoder", "Projector". A green check badge between the two branches reads "Identical architecture". A horizontal arrow from student to teacher is labeled "EMA momentum", and a "stop-gradient" tag with a scissors icon sits on the teacher branch. On the teacher branch only, two extra small boxes appear below the projector, stacked and highlighted in orange: "Centering" and "Sharpening". Both branches then pass through a small box labeled "Softmax" and converge into a pill-shaped badge reading "Cross-entropy loss" in orange.

BOTTOM STRIP: a horizontal three-column mini table with a header row. Row labels on the far left, one short label per cell. Column 1 header "Predictor": cells "BYOL: needed" and "DINO: none". Column 2 header "Loss": cells "BYOL: MSE" and "DINO: cross-entropy". Column 3 header "Anti-collapse": cells "BYOL: predictor + BN" and "DINO: center + sharpen". Use blue text for BYOL cells and orange text for DINO cells.

Style: clean flat vector illustration, educational infographic, white background, thin consistent line weights, rounded rectangles, generous white space, muted blue and orange palette with light gray supporting elements, crisp legible sans-serif typography. No photographic elements, no gradients, no clutter. All labels short and spelled exactly as written.
